.theos/
packages/
release/
repo/
*.log
hotspotpro-bin

# Local config: your links. Set these, then rebuild.
donate-url.txt
repo-url.txt
github-url.txt

# Notes stay on this machine and off GitHub. README.md and RELEASING.md still
# exist locally -- they are ignored, not deleted. If one is ever wanted on
# GitHub again, `git add -f` it.
*.md
restore-md.sh

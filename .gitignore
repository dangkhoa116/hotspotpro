.theos/
packages/
release/
repo/
*.log
hotspotpro-bin

# Local config: your links. Set these, then rebuild.
donate-url.txt
repo-url.txt
github-url.txt

# Notes stay on this machine and off GitHub -- they are ignored, not deleted.
# README.md is the deliberate exception: it is the repo's front page, and the
# Sileo depiction and release post both point people at it to see what the root
# daemon does.
*.md
!README.md
restore-md.sh
